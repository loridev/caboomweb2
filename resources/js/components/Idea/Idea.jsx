import clases from "./styles/Idea.module.css";

function Idea(props) {
    return(
        <section className={props.className}>
            <h2>ABOUT CARLOS</h2>
            <p>info about the game</p>
        </section>
    );
}

export default Idea;