import { toast } from "react-toastify";
import {useEffect} from "react";

function Shop() {
    useEffect(() => {
        toast.error('hola');
    });

    return (
        <div>

        </div>
    )
}

export default Shop;
